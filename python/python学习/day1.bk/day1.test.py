import os

from os  import path



for i in os.listdir('/tmp'):
    if '-' in i : print i
    print a 
    try :
        st = os.stat(filepath)
    except:
        continue           
    print filepath
    print d_size.setdefault(st.st_size, []).append(filepath)