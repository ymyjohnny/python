import os
from os.path import join, getsize

for root, dirs, files in os.walk('/tmp'):
    for file in files:
        if '-' not in file:
            continue
        filenameall = path.join(dirs, file)
        print filenameall

